<?xml version="1.0" encoding="utf-8"?>

<!-- [1] change RelativeLayout to LinearLayout -->
<!-- [1] add the orientation -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="android_workshop.techyon.rockpaperscissors.MainActivity">

    <!-- [1] add your different views here -->
    <!-- [1] name your views so you can differentiate them! -->

    <!-- [1] here's an example ImageView...we still need user hand and score text -->
    <ImageView
        android:id="@+id/computer_hand"
        android:layout_gravity="center"
        android:layout_width="@dimen/image_side_length"
        android:layout_height="@dimen/image_side_length" />
    <Button
        android:id="@+id/rock_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <!-- [1] add the buttons inside another layout (horizontal vs vertical?) -->
</RelativeLayout>
